'use client';
import PageContainer from '@/components/layout/page-container';
import { buttonVariants } from '@/components/ui/button';
import { Heading } from '@/components/ui/heading';
import { Separator } from '@/components/ui/separator';
import { DataTableSkeleton } from '@/components/ui/table/data-table-skeleton';
import { cn } from '@/lib/utils';
import { IconPlus } from '@tabler/icons-react';
import Link from 'next/link';
import { Suspense } from 'react';
import MiniPrizeListingPage from './components/mini-prize-listing';

// export const metadata = {
//   title: 'Dashboard: Mini Prizes'
// };

type PageProps = {};

export default function Page({}: PageProps) {
  return (
    <PageContainer scrollable={false}>
      <div className='flex flex-1 flex-col space-y-4'>
        <div className='flex items-start justify-between'>
          <Heading
            title='Mini Prizes'
            description='Manage Mini Prizes (Server side table functionalities.)'
          />
          <Link
            href='/dashboard/miniprize/new'
            className={cn(buttonVariants(), 'text-xs md:text-sm')}
          >
            <IconPlus className='mr-2 h-4 w-4' /> Add New
          </Link>
        </div>
        <Separator />
        <Suspense
          fallback={
            <DataTableSkeleton columnCount={5} rowCount={8} filterCount={2} />
          }
        >
          <MiniPrizeListingPage />
        </Suspense>
      </div>
    </PageContainer>
  );
}
